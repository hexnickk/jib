[Unit]
Description=Jib Bus (NATS via docker compose)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/sh -c 'docker compose -f /opt/jib/bus/docker-compose.yml up -d'
ExecStop=/bin/sh -c 'docker compose -f /opt/jib/bus/docker-compose.yml down'

[Install]
WantedBy=multi-user.target
