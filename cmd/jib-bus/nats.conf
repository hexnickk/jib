# Managed by jib-bus — do not edit.
# Written to /opt/jib/bus/nats.conf by `jib-bus install`.
#
# NATS runs with defaults from this file. The container exposes port 4222
# only on 127.0.0.1, so local-only access is the primary security boundary.
# Add a token-auth block here when wiring native jib services to depend on
# jib-bus.service instead of the legacy jib-stack compose.
